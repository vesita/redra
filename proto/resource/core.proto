syntax = "proto3";

package resource;
import "resource/formats.proto";
import "resource/shape.proto";

message Resource {
  oneof rsrc {
    formats.Format  format = 1;
     shape.Shape     shape = 2;
  }
}