[package]
name = "redra_parser"
version = "0.1.0"
edition = "2021"

[dependencies]
redra_proto = { path = "../redra_proto" }
bevy = { version = "0.18", optional = true }
nalgebra = "0.34"
log = "0.4"

[features]
default = []
graph = ["bevy"]